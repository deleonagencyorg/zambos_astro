---
// src/pages/[lang]/legacy-redirect.astro
const { currentLang } = Astro.props || { currentLang: 'es' };

return Astro.redirect(`/${currentLang}/`);

export function getStaticPaths() {
  return [];
}
---

